[package]
name = "console"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
font = { path = "../font" }
terminal = { path = "../terminal" }
toyos = { path = "../../toyos" }
window = { path = "../window" }
