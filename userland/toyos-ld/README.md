# toyos-ld

Linker that resolves libraries, combines object files, and produces executables.

Part of the ToyOS native C toolchain alongside toyos-cc. Also used by the toolchain builder to link Rust binaries.
