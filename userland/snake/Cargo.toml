[package]
name = "snake"
version = "0.1.0"
edition = "2021"

[dependencies]
font = { path = "../font" }
rand = "0.8"
winit = { path = "../winit/winit", default-features = false }
softbuffer = { path = "../softbuffer", default-features = false }

[build-dependencies]
fontdue = "0.9"
