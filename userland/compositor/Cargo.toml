[package]
name = "compositor"
version = "0.1.0"
edition = "2021"

[dependencies]
font = { path = "../font" }
image = { version = "0.25", default-features = false, features = ["jpeg"] }
sprite = { path = "../sprite" }
window = { path = "../window" }

[profile.dev]
opt-level = 2
