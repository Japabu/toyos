[package]
name = "toyos-sched-sim"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
toyos-sched = { path = "..", features = ["std"] }
rand = { version = "0.9", features = ["small_rng"] }
