# The QEMU version every CI guest and the dev host run — read by `ci.yml`'s
# shards, `tcg` and `cache-writer`, `gate-a.yml`, and the build system's
# prerequisite check. The version decides test outcomes — 8.2.2 and 11.0.3 were
# measured disagreeing about the same tree — and `debian:sid` is a rolling
# release, so this is a declaration rather than a fact `instrument.sh` could
# read off the image. Changing it is a deliberate act: it says the instrument
# moved.
11.1.0
